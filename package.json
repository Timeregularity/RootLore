{
  "name": "rootlore",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence-backed GitHub issue assistant",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test",
    "evaluate": "node evaluation/run.js",
    "check": "npm test && npm run evaluate && npm --prefix web run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0"
  }
}
